{{>head}}
{{>navbar}}

{{#if message}}
<div class="alert alert-{{message.type}}" role="alert">
    {{message.message}}
</div>
{{/if}}

<div class="container text-center pt-5">
    <form action="" method="post">
        <div>
            <input type="text" name="name" class="field" placeholder="input your name">
        </div>
        <div>
            <input type="email" name="email" class="field" placeholder="input your email">
        </div>
        <div>
            <input type="password" class="field" name="password" placeholder="input your password">
        </div>
        <button type="submit" class="btn-login">Register</button>
    </form>
</div>

{{>footer}}